stat_ellipse <-
function (mapping = NULL, data = NULL, geom = "path", position = "identity", 
    level = 0.75, segments = 51, ...) 
StatEllipse$new(mapping = mapping, data = data, geom = geom, 
    position = position, level = level, segments = segments, 
    ...)

